<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.rog//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 


<mapper namespace="User" >

	<insert id="insertUser" parameterType="com.project.vo.UserVO">
	insert into snsUser (ID,NAME,PASSWORD,BIRTHDAY, SEX) values
	(#{ID},#{NAME},#{PASSWORD},#{BIRTHDAY},#{SEX})
	</insert>

</mapper>